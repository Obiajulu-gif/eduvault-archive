name: Frontend CI

on:
  pull_request:
    paths:
      - "src/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "bun.lock"
      - "next.config.mjs"
      - "eslint.config.mjs"
      - "jsconfig.json"
      - "postcss.config.mjs"
      - ".github/workflows/frontend.yml"
  push:
    branches:
      - main
      - develop
    paths:
      - "src/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "bun.lock"
      - "next.config.mjs"
      - "eslint.config.mjs"
      - "jsconfig.json"
      - "postcss.config.mjs"
      - ".github/workflows/frontend.yml"

jobs:
  frontend:
    name: Lint And Build Frontend
    runs-on: ubuntu-latest
    env:
      MONGODB_URI: mongodb://ci-user:ci-password@mongodb.invalid:27017/eduvault-ci
      MONGODB_DB: eduvault-ci
      JWT_SECRET: ci-only-jwt-secret-not-for-production-1234
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXT_PUBLIC_GATEWAY_URL: https://gateway.pinata.cloud
      PINATA_JWT: ci-pinata-token
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint frontend
        run: npm run lint --if-present -- --ignore-pattern "src/app/api/**" src/app src/components src/hooks src/providers src/utils

      - name: Type check
        run: npm run typecheck --if-present

      - name: Run tests
        run: npm run test:frontend

      - name: Build frontend
        run: npm run build --if-present
